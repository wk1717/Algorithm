/*
#include <iostream>
#define SIZE 100000

class factory
{
	int calculation(int N, int A[100000]);
};

int main()
{
	factory kyo;
	int N;
	int A[SIZE];

	scanf("%d", &N);
	for (int i = 0; i < N; i++)
	{
		scanf("%d", A[i]);
	}
}

int factory::calculation(int N, int A[SIZE])
{
	int number = 0;

	for (int i = 0; i < N; i++)
	{
		if (A[i + 1] == 0)
			number ++;
		else if (A[i + 1] != 0 && A[i + 2] == 0)
			price += 5;
		else if (A[i + 1] !=0 && A[i + 2] != 0)
			price =
	}
}
*/