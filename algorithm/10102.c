/*
#include <stdio.h>

int main()
{
	int v, a = 0, b = 0;
	char user[15];

	scanf("%d", &v);
	for (int i = 0; i < v; i++){
		scanf(" %c", &user[i]);
		if (user[i] == 'A')
			a++;
		else if (user[i] == 'B')
			b++;
	}
	
	if (a > b)
		printf("A");
	else if (a < b)
		printf("B");
	else if (a == b)
		printf("Tie");
	return 0;
}
*/
